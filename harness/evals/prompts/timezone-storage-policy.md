Design and implement the requested timezone storage policy change. Treat it as
an architectural Level 3 task: explore first, record behavior in OpenSpec and
the durable decision in an ADR, then implement with migration rollback and
tests. Do not silently rewrite historical schedule semantics.
