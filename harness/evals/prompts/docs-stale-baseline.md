Correct one stale README/TODO statement that no longer matches the current
runtime. Keep this documentation-only: do not change Rails behavior,
dependencies, schema, or harness rules. Inspect the owning source of truth and
finish with `git diff --check`.
