Implement the approved MVP slice for manually creating a flight. Start with a
small OpenSpec change, keep flight-first scope, use an explicit interactor and
authorization for user-owned data, and defer public API, live tracking, and
offline behavior. Add focused specs and run the project verification targets.
